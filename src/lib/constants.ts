export const DEFAULT_CONTENT = {
  type: "doc",
  content: [
    {
      type: "paragraph",
    },
  ],
};

export const DEFAULT_TAB_NAME = "Overview";

export const AUTOSAVE_DEBOUNCE_MS = 3000;

/** Public brand name — single source of truth. Change here to rebrand. */
export const APP_NAME = "Dealbeam";

/** Primary marketing domain, used in mockup/browser-chrome graphics. */
export const APP_DOMAIN = "dealbeam.com";

/** Resend sender identity ("Brand <address>"). */
export const EMAIL_FROM = `${APP_NAME} <onboarding@resend.dev>`;
